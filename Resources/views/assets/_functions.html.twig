{# Resources/views/assets/_functions.html.twig #}
{% set _DOMAIN = constant("WBW\\Bundle\\DataTablesBundle\\WBWDataTablesBundle::TRANSLATION_DOMAIN") %}
<script type="text/javascript">
    $.fn.dataTable.ext.errMode = function () {
        wbwDataTablesOups();
    }

    /**
     * Oups.
     *
     * @return {undefined}
     */
    function wbwDataTablesOups() {

        if ("function" === typeof swal) {

            swal({
                icon      : "error",
                title     : "{{ "swal.error.title"|trans([], _DOMAIN)|e("js") }}",
                text      : "{{ "swal.error.text"|trans([], _DOMAIN)|e("js") }}",
                dangerMode: true
            });
        }
    }
</script>
